// STUB — LinkedIn Connect not yet implemented
export async function main(
  config: Record<string, unknown>,
  recipientLinkedinUrl: string,
  noteContent?: string,
  context?: { orgId: string; runId: string }
) {
  throw new Error(
    "linkedin-connect node is not yet implemented. Contact the team to enable LinkedIn Connect integration."
  );
}
